# Dashboard (lo que necesita el deploy en Streamlit Community Cloud)
streamlit>=1.40
pandas>=2.2
numpy>=1.26
plotly>=5.24
scikit-learn>=1.5
pyarrow>=15           # lectura del dataset en formato Parquet

# Exportar las gráficas estáticas (PNG) con python main.py
matplotlib>=3.8

# Opcional — solo para exportar el UML a PDF (el SVG se genera sin esto):
#   pip install reportlab svglib
